<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/bgToolbar</item>
        <item name="colorControlNormal">@color/bgToolbar</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MyEditTextTheme">
        <!-- Used for the bottom line when not selected / focused -->
        <item name="colorControlNormal">@color/Xanh</item>
        <!-- colorControlActivated & colorControlHighlight use the colorAccent color by default -->
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colorGray</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <!--<item name="colorAccent">@color/Color Name</item>-->
        <!--<item name="colorControlNormal">@color/Color Name</item>-->
        <!--<item name="colorControlActivated">@color/Color Name</item>-->
    </style>

    <style name="DoiMauRatingBar" parent="AppTheme">
        <item name="colorControlNormal">@color/bgToolbar</item>
    </style>
    <style name="DoiMauRatingBar1" parent="AppTheme">
        <item name="colorControlNormal">@color/bgLinearInToolBar</item>
    </style>





</resources>
